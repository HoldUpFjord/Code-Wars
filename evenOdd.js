function evenOrOdd(number) {
    // input is a single number
    //output is a string;  "Even" if even num or "Odd" if odd num
    // since its binary, a single if/else statement will do. 
      if(number % 2 === 0 ){
        return 'Even'
      } else {
        return "Odd"
      }
    
  }